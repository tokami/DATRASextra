% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fishglob.R
\name{add_total_weight_by_haul_fishglob}
\alias{add_total_weight_by_haul_fishglob}
\title{Add weight estimates to \code{HL} records following the FishGlob workflow}
\usage{
add_total_weight_by_haul_fishglob(x)
}
\arguments{
\item{x}{A \code{datras_raw} object containing \code{HL} and \code{HH} tables.}
}
\value{
A \code{datras_raw} object with the \code{HL} table updated to include the
additional columns:
\itemize{
\item \code{Wgt_indiv}: estimated individual weight in grams,
\item \code{Wgt_total}: estimated total weight for the record in kilograms.
}
}
\description{
Calculate weight-at-length using lookup length-weight parameters from the
species_info table and add the resulting weight fields to the \code{HL} table of
a \code{datras_raw} / \code{DATRASraw} object.
}
\details{
The function merges the \code{HL} table with internal species information to
obtain lookup length-weight parameters (\code{aFG} and \code{bFG}), computes
individual weight from length, and then calculates total weight for each
length class record.

Weight-at-length is calculated for each \code{HL} record as:
\deqn{
  Wgt\_indiv = aFG \times LngtCm^{bFG}
}

where \code{LngtCm} is fish length in centimetres and \code{aFG} and \code{bFG} are
lookup species-specific length-weight parameters obtained from the
internal \code{species_info} data.

Total weight per record is then calculated as:
\deqn{
  Wgt\_total = Wgt\_indiv \times Count
}

Individual weight is first computed in grams and total weight is returned in
kilograms.

Only species belonging to the classes \code{Teleostei}, \code{Elasmobranchii},
\code{Petromyzonti}, and \code{Myxini} are retained in the join with \code{species_info}.

A warning is issued if species are present in the joined data but have
missing length-weight parameters.
}
\examples{
\dontrun{
x <- add_total_weight_by_haul_fishglob(x)
}

}
\seealso{
\code{\link[=add_total_weight_by_haul]{add_total_weight_by_haul()}}
}
