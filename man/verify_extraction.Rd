% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extraction.R
\name{verify_extraction}
\alias{verify_extraction}
\title{Verify a DATRAS archive against its manifest}
\usage{
verify_extraction(path, manifest = NULL, recursive = TRUE, verbose = TRUE)
}
\arguments{
\item{path}{Character string giving the root directory of the archive.}

\item{manifest}{Optional manifest to compare against, as returned by
\code{\link[=read_manifest]{read_manifest()}}. If \code{NULL} (default), the manifest stored in \code{path} is
used.}

\item{recursive}{Logical. If \code{TRUE} (default), search subdirectories.}

\item{verbose}{Logical. If \code{TRUE} (default), print a summary and show a
progress bar.}
}
\value{
A data frame, invisibly, with one row per file and a \code{status}
column taking the values \code{"ok"}, \code{"changed"}, \code{"revised"}, \code{"missing"} or
\code{"new"}.
}
\description{
Recompute the checksum of every exchange file in an archive and compare it
with the manifest, reporting files that have changed, are missing, or are
not listed.
}
\details{
Two kinds of difference are distinguished, because they have different
causes and different remedies:
\itemize{
\item a changed \code{payload_hash} with an unchanged \code{date_of_calculation}
indicates a local problem, such as a corrupted or partially transferred
file,
\item a changed \code{date_of_calculation} indicates that ICES has revised the
data upstream. This is the case that silently changes results and is the
reason the manifest is worth keeping.
}
}
\examples{
\dontrun{
## Check that an archive still matches its manifest
verify_extraction("data/datras")
}

}
\seealso{
\code{\link[=write_manifest]{write_manifest()}}, \code{\link[=read_manifest]{read_manifest()}}, \code{\link[=extraction]{extraction()}}
}
