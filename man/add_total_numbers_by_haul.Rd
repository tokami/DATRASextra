% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/length.R
\name{add_total_numbers_by_haul}
\alias{add_total_numbers_by_haul}
\title{Add total numbers by haul to a DATRAS object}
\usage{
add_total_numbers_by_haul(
  x,
  cm_breaks = NULL,
  by = get_accuracy_cm(x),
  length_cuts = NULL
)
}
\arguments{
\item{x}{A \code{DATRASraw} object.}

\item{cm_breaks}{Numeric vector of break points defining the length classes
in cm. If \code{NULL}, break points are created automatically from the observed
range of \code{HL$LngtCm} using \code{by}. Only used if numbers-at-length must first
be added.}

\item{by}{Numeric scalar giving the width of the default length classes in
cm. Only used when \code{cm_breaks = NULL} and numbers-at-length must first be
added.}

\item{length_cuts}{Optional numeric vector of break points for aggregating
the original length classes into coarser bins after numbers-at-length have
been calculated. Must be strictly increasing.}
}
\value{
A \code{DATRASraw} object with total numbers by haul added to the \code{HH}
component as column \code{HaulN}.
}
\description{
Calculates total numbers for each haul by summing numbers-at-length across
all length classes, and stores the result in the \code{HH} table as \code{HaulN}.
}
\details{
If numbers-at-length are not already present in the object, they are first
created with \code{\link[=add_numbers_at_length]{add_numbers_at_length()}}.

Optionally, the resulting length classes can be aggregated into coarser bins
via \code{length_cuts}.

This function sums the matrix \code{x[["HH"]][["N"]]} over length
classes and writes the result to \code{x[["HH"]][["HaulN"]]}. If \code{length_cuts}
is provided, these original classes are summed into the requested bins
instead of a single vector.

If \code{N} is not yet available, the function calls \code{\link[=add_numbers_at_length]{add_numbers_at_length()}}
first.
}
\examples{

## Add total numbers by haul
x <- add_total_numbers_by_haul(dab)

## Aggregate into broader bins
x <- add_total_numbers_by_haul(dab, length_cuts = c(0, 10, 20, 30, Inf))

}
\seealso{
\code{\link[=add_numbers_at_length]{add_numbers_at_length()}}
}
