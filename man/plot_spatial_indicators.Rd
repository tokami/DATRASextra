% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators.R
\name{plot_spatial_indicators}
\alias{plot_spatial_indicators}
\title{Plot spatial indicators}
\usage{
plot_spatial_indicators(
  x,
  vars = NULL,
  x_var = NULL,
  col = NULL,
  lwd = 2,
  pch = 16,
  legend = TRUE,
  legend_pos = "topright",
  layout = TRUE,
  xlim = NULL,
  main = NULL
)
}
\arguments{
\item{x}{Data frame returned by \code{\link[=calc_spatial_indicators]{calc_spatial_indicators()}}.}

\item{vars}{Character vector of column names to plot. Defaults to all
indicator columns detected in \code{x} (\code{cog_lat} and \code{cog_lon}).}

\item{x_var}{Name of the x-axis column. Defaults to \code{"Year"} if present,
otherwise the first non-result column.}

\item{col}{Character vector of colours, one per unique \code{group}. Defaults to
the DATRASextra discrete palette.}

\item{lwd}{Line width. Default \code{2}.}

\item{pch}{Point symbol. Default \code{16}.}

\item{legend}{Logical; draw a legend for group colours. Default \code{TRUE};
suppressed automatically when there is only one group.}

\item{legend_pos}{Legend position string passed to \code{\link[graphics:legend]{graphics::legend()}}.
Default \code{"topright"}.}

\item{layout}{Logical; when \code{TRUE} (default) and there are multiple
indicator panels, the function sets \code{par(mfrow)} automatically and
restores it on exit. Set to \code{FALSE} to draw panels sequentially into the
caller's current layout -- useful when combining with other plots via
\code{par(mfrow = ...)}.}

\item{xlim}{Optional x-axis limits.}

\item{main}{Optional overall title prefix. If multiple panels are drawn, the
indicator name is appended.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Plots the output of \code{\link[=calc_spatial_indicators]{calc_spatial_indicators()}} as time-series panels, one
panel per indicator variable. When the input contains multiple length groups,
all groups are overlaid as coloured lines within each panel.
}
\examples{
\dontrun{
data(dab)
dab <- add_swept_area(dab)
dab <- add_total_numbers_by_haul(dab)
res <- calc_spatial_indicators(dab, by = "Year")
plot_spatial_indicators(res)
plot_spatial_indicators(res, vars = "cog_lat")
}
}
\seealso{
\code{\link[=calc_spatial_indicators]{calc_spatial_indicators()}}
}
