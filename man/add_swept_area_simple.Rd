% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swept_area.R
\name{add_swept_area_simple}
\alias{add_swept_area_simple}
\title{Add simple swept-area estimates to a \code{datras_raw} object}
\usage{
add_swept_area_simple(
  x,
  min_speed = 1,
  min_dist = 0,
  max_dist_dev = 0.2,
  impute_missing = FALSE,
  width = "WingSpread",
  verbose = TRUE,
  full_report = FALSE
)
}
\arguments{
\item{x}{A \code{datras_raw} object.}

\item{min_speed}{Minimum plausible ground speed. Values below this threshold
are treated as missing. Defaults to \code{1}.}

\item{min_dist}{Minimum plausible towing distance. Values below this
threshold are treated as missing. Defaults to \code{0}.}

\item{max_dist_dev}{Maximum relative deviation allowed between recorded
distance and distance reconstructed from \code{HaulDur} and \code{GroundSpeed}.
Distances with larger deviations are treated as missing. Defaults to \code{0.2}.}

\item{impute_missing}{Logical. Currently included for compatibility, but not
used explicitly in the present implementation.}

\item{width}{Character string specifying which spread column to use as trawl
width. Must be either \code{"WingSpread"} (default) or \code{"DoorSpread"}. Use
\code{"DoorSpread"} for surveys that record door spread but not wing spread.
Note that the fixed beam-trawl widths and the gear-specific plausibility
bounds are only applied when \code{width = "WingSpread"}.}

\item{verbose}{Logical. If \code{TRUE} (default), print a summary of swept-area
missingness and imputation by survey and gear.}

\item{full_report}{Logical. If \code{TRUE}, the summary also includes per-column
missingness counts (\code{na_DoorSpread}, \code{na_WingSpread}, \code{na_Distance},
\code{na_HaulDur}, \code{na_GroundSpeed}) for the original input columns. Defaults
to \code{FALSE}.}
}
\value{
A \code{datras_raw} object with the following fields added to \code{HH}:
\itemize{
\item \code{SweptArea}: swept area based on haul-specific spread,
\item \code{SweptArea_median}: swept area based on gear-specific median spread,
\item \code{SweptArea_imputed}: logical flag, \code{TRUE} when the spread and/or
towing distance used for that haul was imputed.
}
A survey-by-gear summary is attached as \code{attr(x, "swept_area_summary")},
with columns \code{survey}, \code{gear}, \code{n_records}, \code{n_imputed}, \code{prop_imputed}, and
\code{n_NA} / \code{prop_NA} (hauls whose \code{SweptArea} is still \code{NA} after imputation,
e.g. gears that cannot be imputed). When \code{full_report = TRUE}, per-column
missingness counts (\code{na_DoorSpread}, \code{na_WingSpread}, \code{na_Distance},
\code{na_HaulDur}, \code{na_GroundSpeed}) of the original input columns are appended.
}
\description{
Compute haul-level swept-area estimates from towing distance and trawl width
using a simple rule-based approach.
}
\details{
The function uses recorded \code{WingSpread} where available, applies a series of
basic plausibility filters and gear-specific corrections, imputes missing
values from gear-level medians, reconstructs towing distance when needed
from \code{HaulDur} and \code{GroundSpeed}, and returns two swept-area estimates.

Swept area is calculated in \eqn{m^2} as:
\deqn{
  SweptArea = Spread \times Distance
}
where \eqn{Spread} is taken from the column selected by \code{width}.

A second estimate, \code{SweptArea_median}, is also returned. This uses a
gear-specific median spread rather than the haul-specific recorded or
imputed spread:
\deqn{
  SweptArea_median = Spread_median \times Distance
}

The function applies the following steps:
\itemize{
\item removes implausible values of \code{GroundSpeed}, \code{Distance}, and the
selected spread column,
\item removes distance values that differ too much from reconstructed
distance based on speed and haul duration,
\item when \code{width = "WingSpread"}: applies fixed widths for selected beam
trawl gear codes and accounts for double-beam trawls via \code{GearEx == "DB"},
\item imputes missing spread values using gear-specific medians,
\item imputes missing ground speed using gear-specific medians,
\item reconstructs missing towing distance from haul duration and ground
speed.
}
}
\seealso{
\code{\link[=add_swept_area]{add_swept_area()}}, \code{\link[=add_swept_area_fishglob]{add_swept_area_fishglob()}}
}
\keyword{internal}
