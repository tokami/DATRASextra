% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.R
\name{get_latin}
\alias{get_latin}
\title{Get scientific names from WoRMS AphiaIDs}
\usage{
get_latin(x, use_worrms = FALSE)
}
\arguments{
\item{x}{A vector of WoRMS AphiaIDs.}

\item{use_worrms}{Logical; if \code{TRUE}, scientific names are retrieved using
\code{\link[worrms:wm_id2name]{worrms::wm_id2name()}}. If \code{FALSE} (default), AphiaIDs are matched against
the internal \code{species_info} table.}
}
\value{
If \code{use_worrms = FALSE}, a character vector of scientific names with the
same length and order as \code{x}. AphiaIDs not found in \code{species_info} return
\code{NA}, unless \code{x} has length 0, in which case an error is thrown.

If \code{use_worrms = TRUE}, the value returned by \code{\link[worrms:wm_id2name]{worrms::wm_id2name()}}, which
may differ in structure depending on the queried AphiaIDs and the response
from WoRMS.
}
\description{
Retrieves scientific names for one or more WoRMS AphiaIDs. By default,
the function matches AphiaIDs against the internal \code{species_info} table.
Optionally, it can query WoRMS directly via the \pkg{worrms} package.
}
\details{
Using the internal \code{species_info} table provides a fast offline lookup, but
only for AphiaIDs included in that table. Using \code{use_worrms = TRUE} allows
direct lookup from WoRMS, but requires the \pkg{worrms} package to be
installed and may depend on internet access.
}
\examples{
get_latin(126436)

get_latin(c(126436, 126437))

\dontrun{
get_latin(126436, use_worrms = TRUE)
}

}
\seealso{
\code{\link[worrms:wm_id2name]{worrms::wm_id2name()}}
}
