% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download_datras}
\alias{download_datras}
\title{Download ICES DATRAS survey data}
\usage{
download_datras(
  path = NULL,
  surveys = NULL,
  years = NULL,
  overwrite = FALSE,
  download_hl = TRUE,
  download_ca = TRUE,
  use_php = FALSE,
  include_flagged = FALSE,
  return_data = TRUE,
  verbose = TRUE,
  timeout = 10,
  ...
)
}
\arguments{
\item{path}{Character string giving the directory where downloaded files
should be stored. Survey-specific subdirectories are created within this
directory. If \code{NULL}, the current working directory is used.}

\item{surveys}{A character vector of DATRAS survey names to download, for
example \code{"NS-IBTS"} or \code{"BITS"}. If \code{NULL}, all available surveys are
used.}

\item{years}{An integer vector of years to download. If \code{NULL}, all
available years for each selected survey are used.}

\item{overwrite}{Logical. If \code{FALSE} (default), survey-year files that
already exist in \code{path} are skipped. Set to \code{TRUE} to re-download and
overwrite existing files.}

\item{download_hl}{Logical. If \code{TRUE} (default), length-frequency data are
also downloaded where available. This option is only used when \code{use_php = FALSE}.}

\item{download_ca}{Logical. If \code{TRUE} (default), age-length keys and age
data are also downloaded where available. This option is only used when
\code{use_php = FALSE}.}

\item{use_php}{Logical. If \code{FALSE} (default), data are downloaded via
\code{DATRAS::getDatrasExchange()}. If \code{TRUE}, the legacy
\code{DATRAS::downloadExchange()} method is used.}

\item{include_flagged}{Logical. If \code{FALSE} (default), surveys known to be
test surveys or surveys with incomplete data are skipped. Set to \code{TRUE} to
download them anyway.}

\item{return_data}{Logical. If \code{TRUE} (default), the function returns the
downloaded data by running \code{read_datras()} on the specified path.}

\item{verbose}{Logical. If \code{TRUE} (default), progress messages are printed.}

\item{timeout}{Numeric. Maximum number of seconds to wait for the ICES
DATRAS server to respond when retrieving the list of available surveys or
years. If the server does not respond within this time (e.g. because a
firewall blocks the connection), the function falls back to locally cached
information. Default is 10 seconds.}

\item{...}{Additional arguments for the function \code{read_datras()}.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Download ICES DATRAS survey data for one or more surveys and years, and save
each survey-year combination as a zipped exchange file in a survey-specific
subdirectory.
}
\details{
If \code{surveys} is \code{NULL}, all available surveys returned by
\code{icesDatras::getSurveyList()} are used. If \code{years} is \code{NULL}, all available
years for each selected survey are downloaded.

By default, data are downloaded using \code{DATRAS::getDatrasExchange()}, cleaned
to remove extra variables, and written to disk with \code{\link[=write_datras]{write_datras()}}.
Alternatively, the legacy PHP-based download route from
\code{DATRAS::downloadExchange()} can be used by setting \code{use_php = TRUE}.

Files are saved as zipped exchange files named
\code{"<survey>_<year>.zip"} inside survey-specific subfolders.

The following surveys are currently treated as problematic or test surveys:
\code{"NS-IDPS"}, \code{"IS-IDPS"}, \code{"Test-DATRAS"}, and \code{"NS-IBTS_UNIFtest"}.
}
\examples{
\dontrun{
## Download all available years for one survey into the current directory
dat <- download_datras(surveys = "NS-IBTS")

## Download selected years for multiple surveys
dat <- download_datras(
  surveys = c("NS-IBTS", "BITS"),
  years = 2010:2012,
  path = "data/datras"
)

## Re-download existing files
dat <- download_datras(
  surveys = "NS-IBTS",
  years = 2020,
  overwrite = TRUE
)
}

}
