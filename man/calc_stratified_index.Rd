% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index.R
\name{calc_stratified_index}
\alias{calc_stratified_index}
\title{Design-based stratified mean abundance or biomass index}
\usage{
calc_stratified_index(
  x,
  value_var = "HaulN",
  strata_var = "StatRec",
  cpue_method = c("per_swept_area", "per_hour", "count"),
  by = "Year",
  stratum_areas = NULL,
  confidence_level = 0.95
)
}
\arguments{
\item{x}{A \code{datras_raw} object whose \code{HH} table contains \code{value_var}.}

\item{value_var}{Name of the \code{HH} column containing per-haul values.
Default \code{"HaulN"} (total numbers per haul, added by
\code{\link[=add_total_numbers_by_haul]{add_total_numbers_by_haul()}}). Use \code{"HaulWgt"} for a biomass index. The
column may be a numeric vector (single group) or a matrix (one column per
length group); in the latter case one index is computed per column.}

\item{strata_var}{Character vector naming one or more \code{HH} columns that
define strata. Default \code{"StatRec"} (ICES statistical rectangles).}

\item{cpue_method}{Standardisation method applied to \code{value_var}:
\code{"per_swept_area"} (per km^2 swept; requires \code{SweptArea} from
\code{\link[=add_swept_area]{add_swept_area()}}), \code{"per_hour"} (per hour; uses \code{HaulDur}), or \code{"count"}
(no standardisation, raw totals per haul).}

\item{by}{Character vector of \code{HH} column names for which separate indices
are computed (e.g. \code{"Year"}, \code{c("Year", "Quarter")}). Use \code{NULL} for a
single overall index.}

\item{stratum_areas}{Optional data frame with columns matching \code{strata_var}
and a numeric \code{Area} column. If \code{NULL} (default) all strata are weighted
equally.}

\item{confidence_level}{Numeric; coverage probability for the confidence
interval. Default \code{0.95}.}
}
\value{
A data frame with one row per \code{by}-group x length-group combination
and columns: the \code{by} columns (if any), \code{group} (column name from the
value matrix, or \code{value_var} when the column is a vector), \code{cpue_method},
\code{n_hauls}, \code{n_strata}, \code{index}, \code{se}, \code{cv}, \code{ci_lower}, \code{ci_upper}.
}
\description{
Computes a design-based stratified mean index from a \code{datras_raw} object
using pre-computed per-haul totals (\code{HaulN} or \code{HaulWgt}) stored in \code{HH}.
When the value column is a matrix (e.g. numbers split into length groups by
\code{\link[=add_total_numbers_by_haul]{add_total_numbers_by_haul()}}), a separate index and confidence interval are
returned for each column.
}
\details{
The function assumes \code{\link[=clean_datras]{clean_datras()}} has been applied beforehand and that
\code{value_var} is already present in \code{HH} (e.g. via
\code{\link[=add_total_numbers_by_haul]{add_total_numbers_by_haul()}} or \code{\link[=add_weight_at_length]{add_weight_at_length()}}). Hauls where
\code{value_var} is \code{NA} or non-finite after CPUE standardisation are excluded.

The stratified index is:
\deqn{\hat{Y} = \frac{\sum_h A_h \bar{y}_h}{\sum_h A_h}}
with design-based variance
\deqn{\widehat{\mathrm{Var}}(\hat{Y}) =
  \frac{\sum_h A_h^2\, s_h^2 / n_h}{(\sum_h A_h)^2}}
summed over strata with \eqn{n_h \ge 2}. Strata with a single haul
contribute to the index but not to the variance; a warning is issued when
this occurs. Confidence intervals use a t-distribution with degrees of
freedom equal to the number of multi-haul strata minus one.
}
\examples{
\dontrun{
data(dab)
dab <- add_swept_area(dab)
dab <- add_total_numbers_by_haul(dab)
calc_stratified_index(dab, cpue_method = "per_swept_area", by = "Year")

# length-group indices
dab <- add_total_numbers_by_haul(dab, length_cuts = c(15, 25))
calc_stratified_index(dab, cpue_method = "per_swept_area", by = "Year")
}
}
\seealso{
\code{\link[=add_total_numbers_by_haul]{add_total_numbers_by_haul()}}, \code{\link[=add_swept_area]{add_swept_area()}}, \code{\link[=clean_datras]{clean_datras()}}
}
