% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_prune.R
\name{clean_datras}
\alias{clean_datras}
\title{Clean a \code{datras_raw} object}
\usage{
clean_datras(
  x,
  aphias = NULL,
  years = NULL,
  quarters = NULL,
  gears = NULL,
  impute_missing_depth = TRUE,
  correct_species = TRUE,
  do_fishglob = FALSE
)
}
\arguments{
\item{x}{A \code{datras_raw} object.}

\item{aphias}{Optional numeric or character vector of Aphia IDs
(\code{Valid_Aphia}) to retain.}

\item{years}{Optional integer vector of years to retain.}

\item{quarters}{Optional integer vector of quarters to retain.}

\item{gears}{Optional character vector of gear codes to retain.}

\item{impute_missing_depth}{Logical. If \code{TRUE} (default), missing haul depths
are imputed using a smooth spatial GAM fitted to observed depths.}

\item{correct_species}{Logical. If \code{TRUE} (default), species information is
corrected using \code{\link[=correct_species]{correct_species()}}.}

\item{do_fishglob}{Logical. If \code{TRUE}, apply \code{\link[=clean_fishglob]{clean_fishglob()}} instead of
the standard DATRAS cleaning steps.}
}
\value{
A cleaned \code{datras_raw} object.
}
\description{
Apply standard cleaning steps to a \code{datras_raw} / \code{DATRASraw} object.
}
\details{
For standard DATRAS data, the function removes invalid hauls, keeps only
records with complete standard-species recording, optionally imputes missing
haul depths, optionally corrects species information, and can subset the
data by species, year, quarter, or gear.

If \code{do_fishglob = TRUE}, cleaning is delegated to \code{\link[=clean_fishglob]{clean_fishglob()}}.

For standard DATRAS data (\code{do_fishglob = FALSE}), the following filters are
applied before any optional subsetting:
\itemize{
\item only hauls with \code{HaulVal \%in\% c("V", "N")} are retained,
\item only records with \code{StdSpecRecCode == 1} are retained.
}

If \code{impute_missing_depth = TRUE}, missing \code{Depth} values are predicted from a
GAM of \code{log(Depth)} on a two-dimensional smooth of longitude and latitude.

Optional filtering by \code{aphias}, \code{years}, \code{quarters}, and \code{gears} is applied
after cleaning.
}
\examples{
\dontrun{
## Basic cleaning
x_clean <- clean_datras(x)

## Restrict to selected years and quarters
x_clean <- clean_datras(x, years = 2018:2020, quarters = c(1, 3))

## Keep only selected species
x_clean <- clean_datras(x, aphias = c(126417, 126436))
}

}
\seealso{
\code{\link[=prune_datras]{prune_datras()}}, \code{\link[=correct_species]{correct_species()}}, \code{\link[=clean_fishglob]{clean_fishglob()}}
}
