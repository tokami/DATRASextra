% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_datras_overview}
\alias{plot_datras_overview}
\title{Unified DATRAS overview plotting}
\usage{
plot_datras_overview(
  x = NULL,
  mode = c("points", "grid"),
  grid_resolution = c(1, 0.5),
  metric = c("presence", "sum", "mean", "count_hauls", "count_surveys",
    "species_richness"),
  spatial_basis = c("raw", "statrec"),
  by_survey = FALSE,
  by_gear = FALSE,
  by_quarter = FALSE,
  by_year = FALSE,
  by_daynight = FALSE,
  multi_panels = FALSE,
  panel_layout = c("auto", "horizontal", "vertical"),
  value_var = NULL,
  offset_var = NULL,
  positive_only = FALSE,
  transform = c("none", "log1p", "sqrt", "log10"),
  fixed_scale = TRUE,
  fixed_axes = TRUE,
  plot_map = TRUE,
  xlim = NULL,
  ylim = NULL,
  col = NULL,
  palette_rev = FALSE,
  alpha = 0.8,
  pch = 16,
  cex = 0.8,
  size_range = c(0.7, 2.2),
  legend = TRUE,
  legend_mode = c("auto", "none", "global", "per_panel"),
  max_grid_legend_levels = 5,
  legend_ncol = 1,
  legend_pos = NULL,
  legend_cex = NULL,
  grid_group_strategy = c("dominant", "mixed", "error"),
  main = NULL,
  years = NULL,
  asp = "auto",
  add = FALSE
)
}
\arguments{
\item{x}{A DATRAS-like object containing \code{x[['HH']]}, or \code{NULL} to use
\code{DATRASextra::survey_info_full_raw} (fallback \code{survey_info_full}).}

\item{mode}{Plot mode: \code{"grid"} or \code{"points"}. Default is \code{"points"}.}

\item{grid_resolution}{Numeric vector \code{c(lon_step, lat_step)} in degrees
controlling the bin width for \code{mode = "grid"}. Default \code{c(1, 0.5)} gives
the original 1 x 0.5 degree grid. Finer values such as \code{c(0.5, 0.25)} add
spatial detail; coarser values (e.g. \code{c(2, 1)}) reduce clutter for very
dense data. Has no effect in \code{mode = "points"}.}

\item{metric}{Grid metric: \code{"sum"}, \code{"mean"}, \code{"count_hauls"},
\code{"presence"}, \code{"count_surveys"}, or \code{"species_richness"}. The
\code{"species_richness"} option counts unique species (via \code{Valid_Aphia}) per
haul in points mode and per grid cell (pooled across hauls) in grid mode;
it requires \code{x} to contain an \code{HL} table with \code{haul.id} and \code{Valid_Aphia}
columns.}

\item{spatial_basis}{Spatial basis used for coordinates: \code{"raw"} uses haul
coordinates, \code{"statrec"} uses ICES rectangle midpoints from \code{StatRec}.}

\item{by_survey, by_gear, by_quarter, by_year, by_daynight}{Logical grouping
toggles used to define panel groups.}

\item{multi_panels}{Logical. If \code{TRUE}, plot one group per panel.}

\item{panel_layout}{Arrangement of panels when \code{multi_panels = TRUE}. \code{"auto"}
(default) uses \code{\link[grDevices:n2mfrow]{grDevices::n2mfrow()}} to choose a roughly square layout;
\code{"horizontal"} places all panels in a single row; \code{"vertical"} places them
in a single column. For matrix-valued \code{value_var} (length-class panels
crossed with groups), \code{"horizontal"} transposes the row/column assignment.}

\item{value_var}{Optional haul-level variable to map to values.}

\item{offset_var}{Optional haul-level denominator variable.}

\item{positive_only}{Logical. If \code{TRUE}, only hauls with a strictly positive
value are plotted. When \code{value_var} is supplied, positivity is determined
from that variable (after dividing by \code{offset_var} if supplied). When
\code{value_var} is \code{NULL}, the function falls back to total \code{HaulN} or
\code{HaulWgt} from \code{HH} (whichever is found first); a warning is issued if
neither is available. Default \code{FALSE}.}

\item{transform}{Value transform: \code{"none"}, \code{"log1p"}, \code{"sqrt"}, \code{"log10"}.}

\item{fixed_scale}{Logical. If \code{TRUE} (default), a common value scale is used
across groups or panels: in grid mode the colour scale is shared across
panels; in points mode point sizes are scaled relative to the global value
range. If \code{FALSE}, each group (single-panel grouped mode) or each panel
(multi-panel mode) is scaled independently, so point sizes reflect relative
abundance within the group or panel rather than in absolute terms.}

\item{fixed_axes}{Logical. Use common map extent across panels.}

\item{plot_map}{Logical. Add land map layer.}

\item{xlim, ylim}{Optional map limits.}

\item{col}{Optional color palette.}

\item{palette_rev}{Logical. Reverse default palette direction.}

\item{alpha}{Point alpha in points mode.}

\item{pch}{Point symbol.}

\item{cex}{Base point size.}

\item{size_range}{Point size range when value-based scaling is used.}

\item{legend}{Logical. Draw legends.}

\item{legend_mode}{Legend behavior: \code{"auto"}, \code{"none"}, \code{"global"}, or
\code{"per_panel"}.}

\item{max_grid_legend_levels}{Maximum number of levels shown in grid legends.}

\item{legend_ncol}{Number of columns in legend}

\item{legend_pos}{position of legend. Default: NULL.}

\item{legend_cex}{cex of legend. Default: NULL.}

\item{grid_group_strategy}{Strategy for grouped single-panel grid maps when
multiple groups occur in the same cell: \code{"dominant"}, \code{"mixed"}, or
\code{"error"}.}

\item{main}{Optional title for single-panel mode.}

\item{years}{Optional integer vector of years to include. If \code{NULL}
(default), all years in the data are plotted.}

\item{asp}{Aspect ratio passed to \code{\link[graphics:plot]{graphics::plot()}}. \code{"auto"} (default)
uses the latitude-corrected value \code{1 / cos(mean(ylim) * pi / 180)}, which
gives equal physical distances per degree of longitude and latitude at the
mean latitude. Pass \code{1} for a simpler equal-degrees ratio, or \code{NULL} to
leave the aspect ratio determined by the device dimensions (current
behaviour of base \code{plot()}). Fixing \code{asp} prevents the map from distorting
when figure margins or device dimensions change.}

\item{add}{Logical. If \code{FALSE} (default), the function configures its own
graphics device via \code{\link[graphics:par]{graphics::par()}} (\code{mfrow}, \code{mar}, \code{oma}) and restores
the previous settings on exit. If \code{TRUE}, no \code{par()} changes are made: the
plot is drawn into whatever panel or layout is already active, allowing the
function to be embedded in figures created with \code{par(mfrow = ...)} or
\code{\link[graphics:layout]{graphics::layout()}}. When \code{add = TRUE}, the caller is responsible for
setting appropriate margins.}
}
\value{
Invisibly returns list with processed data and plotting metadata.
}
\description{
Plots spatial overviews from DATRAS haul data using gridded image maps or
point maps, with optional grouping and faceting.
}
