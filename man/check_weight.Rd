% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weight.R
\name{check_weight}
\alias{check_weight}
\title{Check and summarize individual weight information in a \code{datras_raw} object}
\usage{
check_weight(x, max_length = NULL, max_weight = NULL, plot = TRUE)
}
\arguments{
\item{x}{A \code{datras_raw} object.}

\item{max_length}{Optional numeric value giving the maximum length in
centimetres to retain for the analysis. Observations above this value are
excluded.}

\item{max_weight}{Optional numeric value giving the maximum individual weight
in grams to retain for the analysis. Observations above this value are
excluded.}

\item{plot}{Logical. If \code{TRUE} (default), produce diagnostic plots of the
observed length-weight relationship and marginal histograms of length and
weight.}
}
\value{
A list with four elements:
\itemize{
\item \code{lPars}: a data frame with summary statistics for observed lengths,
\item \code{wPars}: a data frame with summary statistics for observed weights,
\item \code{parEst}: a data frame with estimated length-weight parameters \code{a}
and \code{b},
\item \code{parEmp}: a data frame with empirical length-weight parameters \code{a}
and \code{b}, if available.
}
}
\description{
Inspect the length-weight information stored in the \code{CA} table of a
\code{datras_raw} / \code{DATRASraw} object.
}
\details{
The function filters to positive individual weights, optionally excludes very
large lengths or weights, summarizes the observed length and weight
distributions, fits a log-log length-weight relationship, and optionally
plots the observed data together with the fitted curve.

The function first calls \code{\link[DATRAS:DATRAS-internal]{DATRAS::checkSpectrum()}} and then works on the \code{CA}
table of the input object.

A linear model of the form
\deqn{
  \log(IndWgt) = \alpha + b \log(LngtCm)
}
is fitted to the filtered observations, and the corresponding length-weight
parameters are returned as:
\deqn{
  a = \exp(\alpha)
}
and
\deqn{
  b
}

If available, empirical length-weight parameters are also retrieved from
\code{species_info} for comparison.
}
\examples{
\dontrun{
res <- check_weight(x)

## Restrict to plausible values
res <- check_weight(x, max_length = 100, max_weight = 10000)
}

}
\seealso{
\code{\link[=check_length]{check_length()}}, \code{\link[DATRAS:DATRAS-internal]{DATRAS::checkSpectrum()}}
}
