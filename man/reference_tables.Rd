% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{reference_tables}
\alias{reference_tables}
\title{Versions of the bundled reference tables}
\usage{
reference_tables(check = TRUE)
}
\arguments{
\item{check}{Logical. If \code{TRUE} (default), hash each table and compare it
with the hash recorded in the registry. Set to \code{FALSE} to skip hashing,
which is faster for large tables.}
}
\value{
A data frame with one row per reference table and the columns
\code{table}, \code{kind}, \code{rows}, \code{columns}, \code{generated}, \code{script}, \code{source}, and,
when \code{check = TRUE}, \code{hash} and \code{status}.
}
\description{
Report which reference tables are distributed with the package, when they
were generated, what they were generated from, and whether they still match
the versions that were registered.
}
\details{
The package ships lookup tables for species and life-history information,
survey coverage, spawning times and ICES area assignment. These are
snapshots of external sources taken at a particular time, so an analysis can
depend on how old they are. This function makes that visible, in the same
way \code{\link[=extraction]{extraction()}} does for the survey data itself.

Two kinds of table are reported. Tables marked \code{"exported"} are documented
data sets that users can load directly, such as \link{species_info}. Tables
marked \code{"internal"} are used by the package but not exported, such as the
statistical-rectangle to ICES-area lookup used by \code{\link[=add_ices_areas]{add_ices_areas()}}.

The example survey data sets (\code{dab}, \code{mini}, \code{wolffish}) are not reference
tables and are not reported here; use \code{\link[=extraction]{extraction()}} on those.

When \code{check = TRUE}, the \code{status} column takes one of:
\itemize{
\item \code{"ok"}: the table matches the registered hash,
\item \code{"changed"}: the table has been regenerated since the registry was
written, so \code{generated} and \code{source} may be out of date,
\item \code{"unregistered"}: the table is present but absent from the registry,
\item \code{"missing"}: the registry lists a table that is not available.
}

Hashes are taken over the serialised object and are comparable within an
installation and between installations of the same package version. They are
intended to detect that a table has been regenerated, not as a
cryptographic guarantee across R versions.
}
\examples{
## Which reference tables are bundled, and how old are they
reference_tables()

## Skip hashing
reference_tables(check = FALSE)

}
\seealso{
\code{\link[=extraction]{extraction()}}, \code{\link[=verify_extraction]{verify_extraction()}}, \code{\link[=add_species_info]{add_species_info()}}
}
