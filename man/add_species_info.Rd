% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.R
\name{add_species_info}
\alias{add_species_info}
\title{Add species information to CA and HL tables}
\usage{
add_species_info(x, vars = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A DATRAS-like object containing at least optional \code{CA} and/or \code{HL}
data frames.}

\item{vars}{Character vector giving the names of columns in \code{species_info}
to add. If \code{NULL}, all columns except \code{WoRMS_AphiaID} are added.}

\item{verbose}{Logical; if \code{TRUE}, progress messages are printed.}
}
\value{
The input object \code{x}, with the requested columns added to the \code{CA} and/or
\code{HL} elements where present.
}
\description{
Adds one or more columns from \code{species_info} to the \code{CA} and/or \code{HL}
elements of a DATRAS-like object by matching \code{Valid_Aphia} in the table
to \code{WoRMS_AphiaID} in \code{species_info}.

The function preserves the original row order of each table and performs
a left join, meaning that rows with no matching species information are
retained and receive \code{NA} in the added columns.

For this type of lookup, the function uses \code{\link[base:match]{base::match()}}, which is
typically faster and simpler than \code{\link[base:merge]{base::merge()}} when only selected
columns need to be appended and the original row order should be kept.
}
\details{
The lookup is based on the correspondence between:
\itemize{
\item \code{x[["CA"]][["Valid_Aphia"]]} or \code{x[["HL"]][["Valid_Aphia"]]}
\item \code{species_info[["WoRMS_AphiaID"]]}
}

If any requested columns already exist in \code{CA} or \code{HL}, they are
overwritten.
}
\examples{
## Add all available species information
## x <- add_species_info(x)

## Add only selected columns
## x <- add_species_info(x, vars = c("ScientificName", "FishBase"))

}
