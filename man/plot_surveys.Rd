% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_surveys}
\alias{plot_surveys}
\title{Plot survey spatial coverage}
\usage{
plot_surveys(
  plot_map = TRUE,
  fixed_axes = TRUE,
  overlay = FALSE,
  consider_quarter = FALSE,
  xlim = NULL,
  ylim = NULL
)
}
\arguments{
\item{plot_map}{Logical. If \code{TRUE} (default), add a coastline map in the
background when the required map packages are installed.}

\item{fixed_axes}{Logical. If \code{TRUE} (default), use the same longitude and
latitude limits for all panels when \code{overlay = FALSE}. If \code{FALSE}, each
survey panel is scaled to its own spatial extent unless overridden by
\code{xlim} or \code{ylim}.}

\item{overlay}{Logical. If \code{FALSE} (default), plot each survey separately.
If \code{TRUE}, overlay all surveys in a single map and show the number of
surveys represented in each spatial cell.}

\item{consider_quarter}{Logical. Only used when \code{overlay = TRUE}. If
\code{TRUE}, survey-quarter combinations are treated as distinct survey units
when counting overlap across space.}

\item{xlim}{Optional numeric vector of length 2 giving the longitude limits
of the plot.}

\item{ylim}{Optional numeric vector of length 2 giving the latitude limits
of the plot.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Plot the spatial footprint of surveys using the internal \code{survey_info_full}
data set.
}
\details{
The function can either show each survey in a separate panel or overlay all
surveys in a single map. In overlay mode, the map shows how many surveys
sampled each spatial cell, optionally distinguishing survey quarters.

When \code{overlay = FALSE}, the function draws the ICES rectangles sampled by
each survey as filled polygons, with one panel per survey.

When \code{overlay = TRUE}, the function aggregates survey occurrence to a regular
longitude-latitude grid derived from ICES rectangle midpoints and displays
the number of overlapping surveys using a heat map.

If \code{consider_quarter = TRUE}, survey-quarter combinations are counted
separately in overlay mode, so the map reflects overlap among
survey-quarter units rather than surveys alone.

The map background is only drawn if both the \code{maps} and \code{mapdata} packages
are available.
}
\examples{
\dontrun{
## Plot each survey separately
plot_surveys()

## Overlay all surveys in one map
plot_surveys(overlay = TRUE)

## Overlay survey-quarter combinations
plot_surveys(overlay = TRUE, consider_quarter = TRUE)

## Allow each panel to use its own spatial extent
plot_surveys(fixed_axes = FALSE)
}

}
\seealso{
\code{\link[=plot_hauls]{plot_hauls()}}, \code{\link[=plot_hauls_by_survey]{plot_hauls_by_survey()}}
}
