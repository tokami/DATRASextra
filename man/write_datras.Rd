% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_write.R
\name{write_datras}
\alias{write_datras}
\title{Write a \code{datras_raw} object to a DATRAS exchange zip file}
\usage{
write_datras(x, zip_file = "DATRAS.zip")
}
\arguments{
\item{x}{A \code{datras_raw} object to be written.}

\item{zip_file}{Character string giving the path and name of the output zip
file. Defaults to \code{"DATRAS.zip"}.}
}
\value{
Invisibly returns the path to the created zip file.
}
\description{
Write the contents of a \code{datras_raw} / \code{DATRASraw} object to a temporary CSV
file in DATRAS exchange format and compress it into a zip archive.
}
\details{
The function writes the available DATRAS components in the order \code{HH}, \code{HL},
and \code{CA}. For each component, the column names are written as a header line,
followed by the corresponding data rows.

The exchange file is first written to a temporary CSV file and then zipped
using \code{\link[utils:zip]{utils::zip()}}. If \code{zip_file} already exists, it is overwritten.

Empty or missing components among \code{HH}, \code{HL}, and \code{CA} are skipped.
}
\examples{
\dontrun{
## Write a DATRAS object to a zip archive
write_datras(x, "NS-IBTS_2020.zip")
}

}
