% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index.R
\name{plot_stratified_index}
\alias{plot_stratified_index}
\title{Plot a stratified abundance or biomass index}
\usage{
plot_stratified_index(
  x,
  x_var = NULL,
  panel_var = NULL,
  ci = TRUE,
  ci_alpha = 0.2,
  log_scale = FALSE,
  fixed_ylim = TRUE,
  col = NULL,
  lwd = 2,
  pch = 16,
  legend = TRUE,
  legend_pos = "topright",
  layout = TRUE,
  xlim = NULL,
  ylim = NULL,
  main = NULL,
  xlab = NULL,
  ylab = NULL
)
}
\arguments{
\item{x}{Data frame returned by \code{\link[=calc_stratified_index]{calc_stratified_index()}}.}

\item{x_var}{Name of the column to use as the x-axis. Defaults to \code{"Year"}
if present, otherwise the first non-result column.}

\item{panel_var}{Optional column name used to split the plot into panels
(e.g. \code{"Survey"}). Default \code{NULL} (single panel).}

\item{ci}{Logical; draw per-group confidence-interval ribbons. Default
\code{TRUE}.}

\item{ci_alpha}{Transparency of the CI ribbons (0-1). Default \code{0.2}.}

\item{log_scale}{Logical; log-transform the y-axis. Default \code{FALSE}.}

\item{fixed_ylim}{Logical; share a common y-axis range across panels.
Default \code{TRUE}.}

\item{col}{Character vector of colours, one per unique \code{group} value.
Defaults to the DATRASextra discrete palette.}

\item{lwd}{Line width. Default \code{2}.}

\item{pch}{Point symbol. Default \code{16}.}

\item{legend}{Logical; draw a legend for the group colours. Default \code{TRUE};
suppressed automatically when there is only one group.}

\item{legend_pos}{Legend position string passed to \code{\link[graphics:legend]{graphics::legend()}}.
Default \code{"topright"}.}

\item{layout}{Logical; when \code{TRUE} (default) and the plot has multiple
panels, the function sets \code{par(mfrow)} automatically and restores it on
exit. Set to \code{FALSE} to draw panels sequentially into the caller's current
layout -- useful when combining with other plots via \code{par(mfrow = ...)}.}

\item{xlim, ylim}{Optional axis limits.}

\item{main}{Optional plot title.}

\item{xlab, ylab}{Axis labels. Auto-generated from \code{cpue_method} if \code{NULL}.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Plots the output of \code{\link[=calc_stratified_index]{calc_stratified_index()}} as a time-series. When the
index contains multiple length groups (from a matrix \code{HaulN}), all groups
are drawn in the same panel as separate coloured lines with a legend. An
optional \code{panel_var} can split the plot into panels (e.g. by survey).
}
\examples{
\dontrun{
data(dab)
dab <- add_swept_area(dab)
dab <- add_total_numbers_by_haul(dab, length_cuts = c(15, 25))
res <- calc_stratified_index(dab, by = "Year")
plot_index(res)
}
}
\seealso{
\code{\link[=calc_stratified_index]{calc_stratified_index()}}
}
