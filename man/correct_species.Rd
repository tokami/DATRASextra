% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_prune.R
\name{correct_species}
\alias{correct_species}
\title{Standardize and correct species information in a \code{datras_raw} object}
\usage{
correct_species(x)
}
\arguments{
\item{x}{A \code{datras_raw} object.}
}
\value{
A \code{datras_raw} object with corrected \code{Species}, \code{Valid_Aphia}, and
\code{Rank} fields in the \code{CA} and \code{HL} tables where applicable.
}
\description{
Correct selected species names, Aphia IDs, and taxonomic ranks in the \code{CA}
and \code{HL} tables of a \code{datras_raw} / \code{DATRASraw} object.
}
\details{
The function is intended to harmonize species information for cases where
records are better treated at genus level, where synonymous or outdated
species names occur, or where taxonomic information is incomplete.

The function operates on the \code{CA} and \code{HL} components, if present and
non-empty.

If the column \code{Rank} is missing, it is added and initialized as
\code{"species"}. Selected taxa are then reassigned to genus level by updating
\code{Species}, \code{Valid_Aphia}, and \code{Rank}. In addition, a small number of species
name corrections are applied.

Examples of corrections include:
\itemize{
\item collapsing selected taxa to genus level, for example
\code{"Dipturus"} or \code{"Gobius"},
\item harmonizing alternative or inconsistent taxonomic names,
\item updating corresponding \code{Valid_Aphia} identifiers,
\item setting \code{Rank} to either \code{"species"} or \code{"genus"} as appropriate.
}

The function currently modifies only the \code{CA} and \code{HL} tables and leaves
\code{HH} unchanged.
}
\examples{
\dontrun{
## Correct species information after reading DATRAS data
x <- correct_species(x)
}

}
\seealso{
\code{\link[=clean_datras]{clean_datras()}}
}
