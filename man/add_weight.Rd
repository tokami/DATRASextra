% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weight.R
\name{add_weight}
\alias{add_weight}
\title{Add weight-at-length estimates to a \code{datras_raw} object}
\usage{
add_weight(
  x,
  per_minute = TRUE,
  max_length = NULL,
  max_weight = NULL,
  empirical = FALSE,
  by_haul = FALSE
)
}
\arguments{
\item{x}{A \code{datras_raw} object.}

\item{per_minute}{Logical. If \code{TRUE} (default), estimated weights are divided
by haul duration in minutes.}

\item{max_length}{Optional numeric value giving the maximum length in
centimetres to retain when fitting the length-weight relationship.
Observations above this value are excluded.}

\item{max_weight}{Optional numeric value giving the maximum individual weight
in grams to retain when fitting the length-weight relationship.
Observations above this value are excluded.}

\item{empirical}{Logical. If \code{TRUE}, use empirical weight-at-length
calculations via \code{\link[=add_weight_empirical]{add_weight_empirical()}} instead of fitting a
length-weight model to the \code{CA} table.}

\item{by_haul}{Logical. If \code{TRUE}, also add haul-level weight information
using \code{\link[=add_weight_by_haul]{add_weight_by_haul()}} or \code{\link[=add_weight_by_haul_empirical]{add_weight_by_haul_empirical()}},
depending on the value of \code{empirical}.}
}
\value{
A \code{datras_raw} object with estimated weight information added.
}
\description{
Estimate catch weight from length data and add the resulting weight fields to
a \code{datras_raw} / \code{DATRASraw} object.
}
\details{
The function derives weight-at-length either from an empirical
length-weight relationship fitted to the \code{CA} table or, if
\code{empirical = TRUE}, from the empirical helper functions
\code{\link[=add_weight_empirical]{add_weight_empirical()}} and optionally \code{\link[=add_weight_by_haul_empirical]{add_weight_by_haul_empirical()}}.

The function first calls \code{\link[DATRAS:DATRAS-internal]{DATRAS::checkSpectrum()}} and then proceeds in one
of two ways:

\itemize{
\item If \code{empirical = FALSE}, a linear model of the form
\eqn{\log(IndWgt) ~ \log(LngtCm)} is fitted to positive individual weights
in the \code{CA} table. Predicted weights are then assigned to length classes
defined by \code{attr(x, "cm.breaks")}, multiplied by numbers-at-length, and
optionally divided by haul duration.

\item If \code{empirical = TRUE}, weight is added using
\code{\link[=add_weight_empirical]{add_weight_empirical()}}. If \code{by_haul = TRUE}, haul-level weight is also
added using \code{\link[=add_weight_by_haul_empirical]{add_weight_by_haul_empirical()}}.
}

When \code{by_haul = TRUE} and \code{empirical = FALSE}, haul-level weight is added
using \code{\link[=add_weight_by_haul]{add_weight_by_haul()}}.
}
\examples{
\dontrun{
## Add fitted weight-at-length estimates
x <- add_weight(x)

## Exclude large values when fitting the length-weight model
x <- add_weight(x, max_length = 100, max_weight = 10000)

## Use empirical weight-at-length instead
x <- add_weight(x, empirical = TRUE)

## Also add haul-level weight
x <- add_weight(x, by_haul = TRUE)
}

}
\seealso{
\code{\link[=check_weight]{check_weight()}}, \code{\link[=add_weight_empirical]{add_weight_empirical()}},
\code{\link[=add_weight_by_haul]{add_weight_by_haul()}}, \code{\link[=add_weight_by_haul_empirical]{add_weight_by_haul_empirical()}}
}
