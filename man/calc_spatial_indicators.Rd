% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators.R
\name{calc_spatial_indicators}
\alias{calc_spatial_indicators}
\title{Centre of gravity from survey data}
\usage{
calc_spatial_indicators(
  x,
  value_var = "HaulN",
  cpue_method = c("per_swept_area", "per_hour", "count"),
  by = "Year",
  lat_var = "lat",
  lon_var = "lon"
)
}
\arguments{
\item{x}{A \code{datras_raw} object. \code{HH} must contain \code{value_var}, \code{lat_var},
and \code{lon_var}.}

\item{value_var}{Name of the \code{HH} column used as CPUE weights. Default
\code{"HaulN"} (added by \code{\link[=add_total_numbers_by_haul]{add_total_numbers_by_haul()}}). May be a numeric
vector or a matrix; in the latter case one set of indicators is computed
per column.}

\item{cpue_method}{Standardisation applied to \code{value_var} before weighting:
\code{"per_swept_area"} (per km^2; requires \code{SweptArea}), \code{"per_hour"} (per
hour; uses \code{HaulDur}), or \code{"count"} (raw totals). Default
\code{"per_swept_area"}.}

\item{by}{Character vector of \code{HH} columns used to define groups (e.g.
\code{"Year"}, \code{c("Year","Quarter")}). Default \code{"Year"}.}

\item{lat_var, lon_var}{Names of the latitude and longitude columns in \code{HH}.
Defaults \code{"lat"} and \code{"lon"} (added by \code{\link[=clean_datras]{clean_datras()}}).}
}
\value{
A data frame with one row per \code{by}-group x length-group and columns:
the \code{by} columns, \code{group}, \code{cpue_method}, \code{n_hauls}, \code{cog_lat}, \code{cog_lon}.
}
\description{
Computes the CPUE-weighted centre of gravity (CoG) from a \code{datras_raw}
object for each level of the \code{by} grouping (typically \code{"Year"}).
}
\details{
The centre of gravity is the CPUE-weighted mean position:
\deqn{\text{CoG}_\text{lat} = \frac{\sum_i w_i \phi_i}{\sum_i w_i},\quad
      \text{CoG}_\text{lon} = \frac{\sum_i w_i \lambda_i}{\sum_i w_i}}
where \eqn{w_i} is the CPUE at haul \eqn{i}. Hauls with zero or missing
CPUE contribute to \code{n_hauls} but not to the weighted sums.
}
\examples{
\dontrun{
data(dab)
dab <- add_swept_area(dab)
dab <- add_total_numbers_by_haul(dab)
calc_spatial_indicators(dab, by = "Year")

## length-group CoG
dab <- add_total_numbers_by_haul(dab, length_cuts = c(0, 15, Inf))
calc_spatial_indicators(dab, by = "Year")
}
}
\seealso{
\code{\link[=add_total_numbers_by_haul]{add_total_numbers_by_haul()}}, \code{\link[=add_swept_area]{add_swept_area()}},
\code{\link[=calc_stratified_index]{calc_stratified_index()}}, \code{\link[=plot_spatial_indicators]{plot_spatial_indicators()}}
}
