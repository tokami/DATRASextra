% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_hauls}
\alias{plot_hauls}
\title{Plot the spatial distribution of hauls}
\usage{
plot_hauls(plot_map = TRUE, xlim = NULL, ylim = NULL)
}
\arguments{
\item{plot_map}{Logical. If \code{TRUE} (default), add a coastline map in the
background when the required map packages are installed.}

\item{xlim}{Optional numeric vector of length 2 giving the longitude limits
of the plot.}

\item{ylim}{Optional numeric vector of length 2 giving the latitude limits
of the plot.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Plot the number of hauls by ICES rectangle on a longitude-latitude grid
using the internal \code{survey_info_full} data set.
}
\details{
The function aggregates haul counts over spatial bins derived from ICES
statistical rectangles and displays them as a heat map. Optionally, a
coastline map is added if the suggested packages \code{maps} and \code{mapdata} are
available.

Hauls are aggregated to a regular grid based on ICES rectangle midpoints.
The resulting frequencies are plotted with \code{\link[=image]{image()}} using a heat-map colour
scale.

The map background is only drawn if both the \code{maps} and \code{mapdata} packages
are available.
}
\examples{
\dontrun{
## Plot all hauls
plot_hauls()

## Plot hauls in a restricted region
plot_hauls(xlim = c(-10, 15), ylim = c(50, 65))
}

}
