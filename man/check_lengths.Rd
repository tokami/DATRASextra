% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/length.R
\name{check_lengths}
\alias{check_lengths}
\title{Check and summarize length distributions in a \code{datras_raw} object}
\usage{
check_lengths(
  x,
  cm_breaks = NULL,
  by = get_accuracy_cm(x),
  length_percentile = 99.9999,
  plot = TRUE
)
}
\arguments{
\item{x}{A \code{datras_raw} object.}

\item{cm_breaks}{Numeric vector of break points for length classes in
centimetres. If supplied, these are used directly to bin \code{LngtCm}.}

\item{by}{Optional bin width in centimetres. If \code{cm_breaks} is not supplied,
the bin width is determined from this argument or, if \code{NULL}, from the
\code{get_accuracy_cm()} function.}

\item{length_percentile}{Numeric percentile used to summarize the upper tail
of the observed length distribution. Defaults to \code{99.9999}.}

\item{plot}{Logical. If \code{TRUE} (default), plot the length spectrum on raw
and log count scales.}
}
\value{
The input \code{datras_raw} object returned invisibly, with a
\code{"length_check"} attribute attached. That attribute is a list with three
elements:
\itemize{
\item \code{N}: a haul-by-length count matrix,
\item \code{lPars}: a data frame of summary statistics for the length
distribution,
\item \code{nAbove}: a data frame with the number and percentage of observations
above selected upper-length thresholds.
}
}
\description{
Create haul-by-length counts from the \code{HL} table of a \code{datras_raw} /
\code{DATRASraw} object and inspect the resulting length distribution.
}
\details{
The function bins fish lengths into user-defined or automatically derived
length classes, constructs a haul-by-length count matrix, computes summary
statistics of the overall length distribution, and optionally plots the
observed spectrum on both raw and log scales.

Lengths are grouped into classes using \code{\link[=cut]{cut()}} and counts are aggregated by
haul and length class.

In addition to the haul-by-length matrix, the function calculates summary
statistics for the pooled length distribution, including:
\itemize{
\item observed minimum length,
\item weighted mean length,
\item median length,
\item observed maximum length,
\item empirical maximum length from \code{species_info}, if available,
\item a user-defined upper percentile of the length distribution.
}

The function also reports the number and percentage of observations above the
empirical maximum length and above the selected percentile threshold.

If multiple species are present in the \code{HL} table, the combined spectrum
across all species is used and a warning is issued.
}
\examples{
\dontrun{
x <- check_lengths(x)
attr(x, "length_check")$lPars

## Use custom length bins
x <- check_lengths(x, cm_breaks = seq(0, 100, by = 1))

## Use an automatically generated spectrum with 0.5 cm bins
x <- check_lengths(x, by = 0.5)
}

}
\seealso{
\code{\link[=check_weights]{check_weights()}}
}
