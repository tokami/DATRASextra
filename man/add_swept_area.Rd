% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swept_area.R
\name{add_swept_area}
\alias{add_swept_area}
\title{Add swept-area estimates to a \code{datras_raw} object}
\usage{
add_swept_area(
  x,
  method = "simple",
  min_speed = 1,
  min_dist = 0,
  max_dist_dev = 0.2,
  impute_missing = FALSE,
  width = "WingSpread",
  verbose = TRUE,
  full_report = FALSE
)
}
\arguments{
\item{x}{A \code{datras_raw} object.}

\item{method}{Character string specifying the method used to calculate swept
area. Must be either \code{"simple"} (default) or \code{"fishglob"}.}

\item{min_speed}{Minimum plausible ground speed. Values below this threshold
are treated as missing. Only used when \code{method = "simple"}. Defaults to
\code{1}.}

\item{min_dist}{Minimum plausible towing distance. Values below this
threshold are treated as missing. Only used when \code{method = "simple"}.
Defaults to \code{0}.}

\item{max_dist_dev}{Maximum relative deviation allowed between recorded
distance and distance reconstructed from \code{HaulDur} and \code{GroundSpeed}.
Distances with larger deviations are treated as missing. Only used when
\code{method = "simple"}. Defaults to \code{0.2}.}

\item{impute_missing}{Logical. Only used when \code{method = "simple"}. Defaults
to \code{FALSE}.}

\item{width}{Character string specifying which spread column to use as trawl
width. Must be either \code{"WingSpread"} (default) or \code{"DoorSpread"}. Only
used when \code{method = "simple"}; ignored for \code{method = "fishglob"}.}

\item{verbose}{Logical. If \code{TRUE} (default) and \code{method = "simple"}, print a
summary of swept-area missingness and imputation by survey and gear.}

\item{full_report}{Logical. If \code{TRUE}, the printed and attached summary also
includes per-column missingness counts (\code{na_DoorSpread}, \code{na_WingSpread},
\code{na_Distance}, \code{na_HaulDur}, \code{na_GroundSpeed}) for the original input
columns. Only used when \code{method = "simple"}. Defaults to \code{FALSE}.}
}
\value{
A \code{datras_raw} object with swept-area estimates added.
}
\description{
Compute haul-level swept-area estimates for a \code{datras_raw} / \code{DATRASraw}
object using one of the supported methods.
}
\details{
This function is a wrapper around an internal simple swept-area function and
\code{\link[=add_swept_area_fishglob]{add_swept_area_fishglob()}}. It allows users to choose between a simple
rule-based approach and the FishGlob workflow.

If \code{method = "simple"}, swept area is calculated using a simple rule-based
approach that applies basic plausibility filters, gear-specific median
imputations, and a direct distance-times-width calculation. A per-haul
logical column \code{SweptArea_imputed} is added to \code{HH}, and a survey-by-gear
summary of missingness and imputation is attached as
\code{attr(x, "swept_area_summary")} (and printed when \code{verbose = TRUE}).

If \code{method = "fishglob"}, swept area is calculated using
\code{\link[=add_swept_area_fishglob]{add_swept_area_fishglob()}}, which follows the FishGlob workflow based on
survey-specific spread models and hierarchical fallback rules.
}
\examples{
\dontrun{
## Simple swept-area estimation
x <- add_swept_area(x)

## FishGlob workflow
x <- add_swept_area(x, method = "fishglob")

## Simple method with stricter filters
x <- add_swept_area(x, method = "simple", min_speed = 2,
                    max_dist_dev = 0.1)
}

}
\seealso{
\code{\link[=add_swept_area_fishglob]{add_swept_area_fishglob()}}
}
