% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_hauls_by_survey}
\alias{plot_hauls_by_survey}
\title{Plot the spatial distribution of hauls for each survey}
\usage{
plot_hauls_by_survey(
  plot_map = TRUE,
  fixed_scale = TRUE,
  col = hcl.colors(12, "YlOrRd", rev = TRUE),
  xlim = NULL,
  ylim = NULL
)
}
\arguments{
\item{plot_map}{Logical. If \code{TRUE} (default), add a coastline map in the
background when the required map packages are installed.}

\item{fixed_scale}{Logical. If \code{TRUE} (default), use a common colour scale
across surveys to make panels directly comparable. If \code{FALSE}, each survey
gets its own scale based on its maximum haul density.}

\item{col}{A vector of colours used for the heat maps. Defaults to
\code{hcl.colors(12, "YlOrRd", rev = TRUE)}.}

\item{xlim}{Optional numeric vector of length 2 giving the longitude limits
of the plots.}

\item{ylim}{Optional numeric vector of length 2 giving the latitude limits
of the plots.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Plot haul density by location separately for each survey using the internal
\code{survey_info_full} data set.
}
\details{
The function aggregates haul counts over spatial bins derived from ICES
statistical rectangles and displays them as heat maps in a multi-panel
layout, with one panel per survey. Optionally, a coastline map is added if
the suggested packages \code{maps} and \code{mapdata} are available.

Hauls are aggregated to a regular grid based on ICES rectangle midpoints.
For each survey, the resulting frequencies are plotted with \code{\link[=image]{image()}} using a
heat-map colour scale.

If \code{fixed_scale = TRUE}, all panels use the same class breaks, allowing
direct comparison of haul intensity among surveys. If \code{FALSE}, class breaks
are determined separately for each survey.

The map background is only drawn if both the \code{maps} and \code{mapdata} packages
are available.
}
\examples{
\dontrun{
## Plot haul density for all surveys
plot_hauls_by_survey()

## Use survey-specific colour scales
plot_hauls_by_survey(fixed_scale = FALSE)

## Restrict the plotted region
plot_hauls_by_survey(xlim = c(-10, 15), ylim = c(50, 65))
}

}
\seealso{
\code{\link[=plot_hauls]{plot_hauls()}}, \code{\link[=plot_surveys]{plot_surveys()}}
}
