% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fishglob.R
\name{as_fishglob}
\alias{as_fishglob}
\title{Convert a \code{datras_raw} object to FishGlob format}
\usage{
as_fishglob(x)
}
\arguments{
\item{x}{A \code{datras_raw} object containing \code{HH} and \code{HL} tables.}
}
\value{
A data frame in FishGlob-like format, with one row per
haul-taxon combination.
}
\description{
Convert a \code{datras_raw} / \code{DATRASraw} object into a data frame structured to
match the FishGlob data set format.
}
\details{
The function aggregates length-based catch information in \code{HL} to the
haul-species level, merges the result with haul-level metadata from \code{HH},
computes abundance and biomass standardization metrics, and renames selected
fields to match FishGlob naming conventions.

The function first aggregates \code{HL} records by haul and taxon using:
\itemize{
\item total weight (\code{wgt}), calculated as the sum of \code{Wgt_total},
\item total number (\code{num}), calculated as the sum of \code{Count}.
}

These aggregated values are merged with the \code{HH} table and used to compute:
\itemize{
\item \code{wgt_cpue}: biomass per hour trawled,
\item \code{wgt_cpua}: biomass per swept area,
\item \code{num_cpue}: abundance per hour trawled,
\item \code{num_cpua}: abundance per swept area.
}

Haul duration is converted from minutes to hours before calculating CPUE
metrics.

The returned data frame follows FishGlob naming conventions where possible,
including fields such as \code{survey}, \code{haul_id}, \code{latitude}, \code{longitude},
\code{accepted_name}, and \code{aphia_id}.

Some output fields are created as placeholders with missing values when the
corresponding information is not available in the input data.

Country and survey-unit fields are assigned using survey-specific rules that
approximate the FishGlob workflow.
}
\examples{
\dontrun{
fg <- as_fishglob(x)
}

}
\seealso{
\code{\link[=clean_fishglob]{clean_fishglob()}}, \code{\link[=prune_fishglob]{prune_fishglob()}}, \code{\link[=add_swept_area_fishglob]{add_swept_area_fishglob()}},
\code{\link[=add_total_weight_by_haul_fishglob]{add_total_weight_by_haul_fishglob()}}
}
