% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fishglob.R
\name{add_swept_area_fishglob}
\alias{add_swept_area_fishglob}
\title{Add swept-area estimates following the FishGlob workflow}
\usage{
add_swept_area_fishglob(x)
}
\arguments{
\item{x}{A \code{datras_raw} object containing haul-level data in \code{HH}.}
}
\value{
A \code{datras_raw} object with two additional columns in \code{HH}:
\itemize{
\item \code{SweptArea}: swept area in \eqn{km^2},
\item \code{DoorsArea}: door-swept area in \eqn{km^2}.
}
}
\description{
Compute haul-level swept-area estimates for a \code{datras_raw} / \code{DATRASraw}
object using the FishGlob methodology.
}
\details{
The function adds haul-level swept area (\code{SweptArea}) and door-swept area
(\code{DoorsArea}) to the \code{HH} table. Missing \code{WingSpread}, \code{DoorSpread}, and
\code{Distance} values are reconstructed using survey-specific prediction models
and hierarchical fallback rules.

The implementation follows the methodology used in the FishGlob workflow.
Missing \code{WingSpread} and \code{DoorSpread} values are predicted from internally
stored survey-specific models. When observed values are missing or judged
implausible, the function applies survey-specific corrections and fallback
prediction rules.

Missing towing distance is reconstructed from \code{GroundSpeed} and \code{HaulDur}.
Missing \code{GroundSpeed} values are imputed hierarchically using averages at
the following levels:
\itemize{
\item survey-year-ship,
\item survey-year-country,
\item survey-country,
\item global mean.
}

Swept area is then computed as:
\deqn{
  SweptArea = Distance \times WingSpread \times 10^{-6}
}

Door-swept area is computed analogously as:
\deqn{
  DoorsArea = Distance \times DoorSpread \times 10^{-6}
}

Both quantities are returned in \eqn{km^2}, assuming \code{Distance},
\code{WingSpread}, and \code{DoorSpread} are recorded in metres.

Some survey- and gear-specific filtering and manual corrections are applied
to match the FishGlob workflow.

The original spread-estimation logic was developed by Aurore Maureaud and
P. Daniël van Denderen and is available from the FishGlob code repository.
}
\examples{
\dontrun{
x <- add_swept_area_fishglob(x)
}

}
\references{
FishGlob workflow code:
\url{https://github.com/fishglob/FishGlob_data/blob/main/cleaning_codes/source_DATRAS_wing_doorspread.R}
}
\seealso{
\code{\link[=add_swept_area]{add_swept_area()}}, \code{\link[=clean_fishglob]{clean_fishglob()}}
}
