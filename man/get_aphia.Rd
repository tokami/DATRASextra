% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.R
\name{get_aphia}
\alias{get_aphia}
\title{Get WoRMS AphiaIDs from species names}
\usage{
get_aphia(x, use_worrms = FALSE)
}
\arguments{
\item{x}{A character vector of scientific species names.}

\item{use_worrms}{Logical; if \code{TRUE}, AphiaIDs are retrieved using
\code{\link[worrms:wm_name2id]{worrms::wm_name2id()}}. If \code{FALSE} (default), names are matched against
the internal \code{species_info} table.}
}
\value{
If \code{use_worrms = FALSE}, an integer vector of WoRMS AphiaIDs with the same
length and order as \code{x}. Names not found in \code{species_info} return \code{NA}.

If \code{use_worrms = TRUE}, the value returned by \code{\link[worrms:wm_name2id]{worrms::wm_name2id()}}, which
may differ in structure from the internal lookup result depending on the
queried names and the response from WoRMS.
}
\description{
Retrieves WoRMS AphiaIDs for one or more scientific species names.
By default, the function matches names against the internal
\code{species_info} table. Optionally, it can query the \pkg{worrms}
package to look up AphiaIDs directly from WoRMS.
}
\details{
Using the internal \code{species_info} table provides a fast offline lookup, but
only for names included in that table. Using \code{use_worrms = TRUE} allows
direct lookup from WoRMS, but requires the \pkg{worrms} package to be
installed and may depend on internet access.
}
\examples{
get_aphia("Gadus morhua")

get_aphia(c("Gadus morhua", "Melanogrammus aeglefinus"))

\dontrun{
get_aphia("Gadus morhua", use_worrms = TRUE)
}

}
\seealso{
\code{\link[worrms:wm_name2id]{worrms::wm_name2id()}}
}
