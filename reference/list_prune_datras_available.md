# Available columns in a datras_raw object

Available columns in a datras_raw object

## Usage

``` r
list_prune_datras_available(x)
```

## Arguments

- x:

  A `datras_raw` object.

## Value

A named list with column names in `HH`, `HL`, and `CA`.
